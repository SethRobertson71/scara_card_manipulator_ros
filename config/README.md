# Config Directory

This directory is mounted into the container at /ros2_ws/config.

Place the following files here as your system develops:
  - camera_calibration.yaml   (after completing camera calibration)
  - cyclonedds.xml            (if customizing DDS settings)
  - workspace_bounds.yaml     (custom pick/place workspace definition)
